
#define MAX_SENTENCE_LENGTH 1024

#define ARRAY_LENGTH(array) (sizeof((array))/sizeof((array)[0]))

char* _checksum_str(const char * s, char* checksum);

size_t zzz_strlcpy(char * dst, const char * src, size_t dsize);
